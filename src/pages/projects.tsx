import { navLinks } from "../nav/nav_links";

function Projects() {
  return (
    <div
      id={navLinks.project.destination}
      className="layer"
    >
      projects
    </div>
  );
}

export default Projects;
